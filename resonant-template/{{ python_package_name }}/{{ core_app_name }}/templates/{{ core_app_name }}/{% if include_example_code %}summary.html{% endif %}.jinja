{% raw -%}
<!doctype html>
<html lang="">
  <head>
    <meta charset="utf-8">
  </head>

  <body>
    <table>
      {% for user in user_summary %}
        <tr>
          <td>{{ user.email }}</td>
          <td>{{ user.processed_images }} ({{ user.unprocessed_images }} processing)</td>
        </tr>
      {% endfor %}
    </table>
  </body>
</html>
{% endraw %}
