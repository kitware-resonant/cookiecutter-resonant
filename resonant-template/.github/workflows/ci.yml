name: ci
on:
  pull_request:
  push:
    branches:
      - master
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-24.04
    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_DB: django
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd "pg_isready --username postgres"
          --health-start-period 30s
          --health-start-interval 2s
        ports:
          - 5432:5432
      rabbitmq:
        image: rabbitmq:4.3-management-alpine
        options: >-
          --health-cmd "rabbitmq-diagnostics ping"
          --health-start-period 30s
          --health-start-interval 2s
        ports:
          - 5672:5672
      seaweedfs:
        image: chrislusf/seaweedfs:4.47
        env:
          AWS_ACCESS_KEY_ID: seaweedAccessKey
          AWS_SECRET_ACCESS_KEY: seaweedSecretKey
        options: >-
          --health-cmd "curl --fail --silent http://localhost:8333/healthz"
          --health-start-period 30s
          --health-start-interval 2s
        ports:
          - 8333:8333
    steps:
      - name: Checkout repository
        uses: actions/checkout@v0
      - name: Install uv
        uses: astral-sh/setup-uv@v0
      - name: Run tests
        run: |
          uv run --locked tox
        env:
          DJANGO_DATABASE_URL: postgres://postgres:postgres@localhost:5432/django
          DJANGO_CELERY_BROKER_URL: amqp://localhost:5672/
          DJANGO_MINIO_STORAGE_URL: http://seaweedAccessKey:seaweedSecretKey@localhost:8333/django-storage
